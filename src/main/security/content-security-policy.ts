/**
 * This policy permits only application-owned resources and rejects plugins, frames, forms, remote connections,
 * inline scripts, and dynamic code evaluation. It is shared by the packaged protocol response and renderer HTML.
 */
export const CONTENT_SECURITY_POLICY = [
  "default-src 'self'",
  "script-src 'self'",
  "style-src 'self'",
  "img-src 'self' data:",
  "font-src 'self'",
  "connect-src 'self'",
  "object-src 'none'",
  "base-uri 'none'",
  "form-action 'none'",
  "frame-ancestors 'none'",
].join('; ');
